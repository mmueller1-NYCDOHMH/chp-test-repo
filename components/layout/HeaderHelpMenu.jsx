/**
 * FILE: HeaderHelpMenu.jsx
 *
 * DEPRECATED — not imported anywhere. Safe to delete this file.
 *
 * This combined "?" button (About this tool + keyboard shortcuts in one
 * dropdown) lived briefly in the site header. It's been superseded by three
 * separate controls in StickyContextBar's right-hand utility group:
 *   - a plain "About this tool" text link
 *   - LanguageToggle
 *   - KeyboardShortcutsButton (shortcuts only, About split back out)
 *
 * I couldn't run a shell `rm` in this session (sandbox was out of disk
 * space), so this file was emptied down to this note instead of removed
 * outright. Delete it whenever convenient — nothing references it.
 */
export {};
