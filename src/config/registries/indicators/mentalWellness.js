/**
 * FILE: indicators/mentalWellness.js
 *
 * PURPOSE:
 * Indicator definitions for the Mental Wellness subcategory,
 * under the Social & Economic Wellness nav category.
 *
 * STATUS: Placeholder — indicators TBD. Add entries here once confirmed with BES.
 *
 * ADDING AN INDICATOR:
 * 1. Add a data file at /data/indicators/{key}.json
 * 2. Add an entry below following the same shape
 * 3. Reference it in /config/sections/mentalWellness.js via asChartConfig()
 * No component code changes required.
 *
 * FIELDS: see /config/registries/indicatorRegistry.js for full field docs.
 */

import { MENTAL_WELLNESS_ID } from '../sectionIds';

// Indicators TBD — this object is intentionally empty until confirmed with BES.
export const mentalWellnessIndicators = {};
